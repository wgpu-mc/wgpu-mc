package eapi.buffer;

import eapi.EBindable;

/**
 * Represents a buffer which has been specified to be able to be used as an SSBO
 */
public interface EBindableBuffer extends EBuffer, EBindable {

}
